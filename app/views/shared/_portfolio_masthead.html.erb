<section class="jumbotron text-center">
  <div class="container">
    <h1 class="jumbotron-heading">My Portfolio</h1>
    <p class="lead text-muted">This is a collection of some of my accomplishments, projects, and skills.</p>
    <p>
      <%= link_to "Find Out More About Me", about_path, class: 'btn btn-success' %>
      <%= link_to "My Blog", blogs_path, class: 'btn btn-secondary' %>
    </p>
  </div>
</section>

<div class="container">
  <div class="row">
    <% if logged_in?(:site_admin) %>
      <div class='btn btn-success btn-block' id="new-portfolio">
        <%= link_to "Create New Portfolio Item", new_portfolio_path %>
      </div>
    <% end %>
  </div>
</div>
