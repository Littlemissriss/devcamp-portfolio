<section class="jumbotron text-center">
  <div class="container">
    <h1 class="jumbotron-heading">Album example</h1>
    <p class="lead text-muted">Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
    <p>
      <%= link_to "Find Out More About Me", about_path, class: 'btn btn-primary' %>
      <%= link_to "My Blog", blogs_path, class: 'btn btn-secondary' %>
    </p>
  </div>
</section>

<div class="container">
  <div class="row">
    <% if logged_in?(:site_admin) %>
      <div class='btn btn-primary btn-block' id="new-portfolio">
        <%= link_to "Create New Portfolio Item", new_portfolio_path %>
      </div>
    <% end %>
  </div>
</div>
